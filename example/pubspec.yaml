name: prism_gradients_toolkit_example
description: Example app demonstrating prism_gradients_toolkit widgets
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  prism_gradients_toolkit:
    path: ../ # For development - change to ^0.0.1 when published

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true